name: news_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^13.0.1
  flutter_bloc: ^8.1.3
  bloc: ^8.0.0
  dio: ^5.4.0
  rxdart: ^0.27.7
  sqflite: ^2.3.0

  news_lib:
    path: ../../libs/news_lib

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
